# ipaddress в Python

Модуль позволяет работать с IP-адресами и сетями как с объектами. В этом разделе рассматриваем только IPv4: адреса из четырёх чисел от 0 до 255 и маски подсети.

## Основные функции и классы

| Инструмент | Что делает |
| --- | --- |
| `ip_address(строка)` | Объект адреса; например, ip_address('192.168.1.5'). |
| `ip_network(строка)` | Объект сети, заданной адресом сети и маской. |
| `ip_network(строка, strict=False)` | Определить сеть по адресу узла и маске. |
| `network_address`, `broadcast_address` | Первый и последний адрес сети. |
| `netmask`, `prefixlen`, `num_addresses` | Маска, число единиц в маске, число всех адресов. |
| `address in network`, `network.hosts()` | Проверка принадлежности; перебор адресов узлов. |

## Как пользоваться

Запись `/24` означает 24 единицы в 32-битной маске: для адресной части остаётся 8 бит, всего в сети 2 ** 8 = 256 адресов. Маску можно записать и как `255.255.255.0`.

По умолчанию `ip_network` ожидает именно адрес сети: `ip_network('192.168.1.5/24')` вызовет ошибку. При `strict=False` адрес узла преобразуется в адрес сети.

Цикл `for address in network` включает все адреса, в том числе адрес сети и широковещательный. `hosts()` для обычных IPv4-сетей с префиксом до /30 исключает эти два адреса. У /31 оба адреса считаются узлами, у /32 — единственный. Не вычитайте 2 автоматически из любого размера сети.

Для двоичной записи используйте `format(int(address), '032b')`: получается ровно 32 бита с ведущими нулями. Обычный `bin` добавляет `0b` и не сохраняет ведущие нули. Большие сети нельзя бездумно превращать в список.

## Типовые школьные задачи

Определить адрес сети по адресу узла; проверить принадлежность адреса подсети; найти число адресов; посчитать адреса с нужным количеством единиц в двоичной записи. Всегда уточняйте, требует условие все адреса сети или только адреса узлов.

## Где встречается в ЕГЭ/ОГЭ

Основная связь — [ЕГЭ №10 — IP-адреса и маски](../../../ЕГЭ/10_IP-адреса_и_маски_подсети/) по текущей структуре библиотеки. В [ОГЭ №7 — адреса в Интернете](../../../ОГЭ/07_Адреса_в_Интернете/) модуль может помогать при проверке учебных примеров IPv4, но не заменяет понимание адресов и не предназначен для сборки URL.

Номера приведены по [текущему оглавлению ЕГЭ](../../../ЕГЭ/README.md), ориентированному на проект 2027 года. Модуль — инструмент решения, а не отдельное требование экзамена.

## Короткие примеры

### Сеть по адресу узла

```python
from ipaddress import ip_network

network = ip_network('192.168.1.5/24', strict=False)
print(network.network_address)  # 192.168.1.0
print(network.netmask)  # 255.255.255.0
```

### Границы и размер сети

```python
from ipaddress import ip_network

network = ip_network('192.168.1.0/30')
print(network.broadcast_address)  # 192.168.1.3
print(network.prefixlen)  # 30
print(network.num_addresses)  # 4
```

### Принадлежность сети

```python
from ipaddress import ip_address, ip_network

network = ip_network('192.168.1.0/24')
print(ip_address('192.168.1.200') in network)  # True
print(ip_address('192.168.2.1') in network)  # False
```

### Все адреса и адреса узлов

```python
from ipaddress import ip_network

network = ip_network('192.168.1.0/30')
print([str(address) for address in network])
# ['192.168.1.0', '192.168.1.1', '192.168.1.2', '192.168.1.3']
print([str(address) for address in network.hosts()])
# ['192.168.1.1', '192.168.1.2']
```

### Двоичная запись из 32 бит

```python
from ipaddress import ip_address

address = ip_address('192.168.1.1')
bits = format(int(address), '032b')
print(bits)  # 11000000101010000000000100000001
print(bits.count('1'))  # 7
```

### Адреса с чётным числом единиц

```python
from ipaddress import ip_network

network = ip_network('192.168.1.0/29')
count = 0
# По условию этого примера рассматриваем ВСЕ адреса сети.
for address in network:
    bits = format(int(address), '032b')
    if bits.count('1') % 2 == 0:
        count += 1
print(count)  # 4
```

## Частые ошибки

- Забывать `strict=False`, когда дан адрес узла.
- Исключать первый и последний адрес, хотя требуется перебрать всю сеть.
- Считать точки частью двоичного представления адреса.
- Перебирать огромную сеть вместо анализа битов.
- Использовать строки вместо объектов при проверке принадлежности сети.

## Памятка

- `ip_address(строка)` — Объект адреса; например, ip_address('192.168.1.5').
- `ip_network(строка)` — Объект сети, заданной адресом сети и маской.
- `ip_network(строка, strict=False)` — Определить сеть по адресу узла и маске.
- `network_address`, `broadcast_address` — Первый и последний адрес сети.
- `netmask`, `prefixlen`, `num_addresses` — Маска, число единиц в маске, число всех адресов.
- `address in network`, `network.hosts()` — Проверка принадлежности; перебор адресов узлов.

Готовые независимые блоки: [`ipaddress.py`](ipaddress.py). Каждый содержит собственные импорты и данные.

Копируйте блок в `solution.py` **в отдельной папке**. Имя учебного файла совпадает с именем модуля и при обычном запуске может помешать импорту. Для запуска всего учебного файла из его папки используйте `python -I ipaddress.py`; флаг `-I` исключает папку скрипта из поиска модулей. Все примеры маленькие, не требуют ввода и могут выполняться вместе.

[Оглавление раздела](../README.md) · [Документация Python](https://docs.python.org/3/library/ipaddress.html)
